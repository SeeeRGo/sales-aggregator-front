export enum MessageStatus {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
  INTERESTING = "INTERESTING",
}

export enum ProcessStatus {
  PENDING = "PENDING",
  PROCESSED = "PROCESSED",
  DELETED = "DELETED",
}
