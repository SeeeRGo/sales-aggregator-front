export enum MessageStatus {
  PENDING = "PENDING",
  APPROVED = "INTERESTING",
  REJECTED = "IGNORED",
  INTERESTING = "POTENTIAL",
  COPY = "COPY",
}

export enum ProcessStatus {
  PENDING = "PENDING",
  PROCESSED = "PROCESSED",
  DELETED = "DELETED",
}

